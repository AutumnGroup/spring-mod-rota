----- mission spirits settigns ------
----- more about: http://springrts.com/phpbb/viewtopic.php?f=55&t=28259

local spGetUnitPosition	= Spring.GetUnitPosition

newPlan = {
}

newSpiritDef = {
	["transporter"] = function(groupID, teamNumber, mode)
	    if (mode == "prepare") then
		    return -- do nothing
		else  -- execute mode:
		
		end
    end,
}